=== PLAN STRENGTHS ===
The plan's skeleton is confirmed sound by all five lenses and by direct code verification, and should not be churned: the throwaway-model identity with from-scratch retrain, the one-pass/no-self-training rule (§7.4), the M2/ordinal scope fence with the DINOv2 hand-off (§7.1), and the frozen-backbone DFR-only tuning regime with full-FT prohibited (§3.3) are all correct, literature-honest, and settled. The DFR premise is verified in code — a single nn.Linear over post-norm CLS means a scoped row refit provably cannot move the other ~19.2K tags — and the swap-not-remove design, additive bias, tiered human-queue disposition, sealed one-shot verification concept, decision to not persist the full 240GB score matrix, uniform-448 no-resolution-mixing principle, and audit-before-cache-rebuild sequencing are all right and retained by every edit above. The build-once cache discipline and the two-user-decision-point structure survive intact (one gate is added, none removed), and one piece of good news should be written in rather than re-litigated: the loss-state persistence blocker is already implemented in the working tree.

=== VERDICTS (30) ===
[ADOPT-MODIFIED] (critical) [literature + domain-shift] Replay batches feed the refit the noise being cleaned (lit-1 + domain-1, merged)
    Refutation attempt: replay's stated purpose is domain anchoring, and ~90% of replay group labels are correct. But the refit exists precisely to move the contested edges, and on those edges 50% of each batch pulls back toward convention with plain BCE (the ASL missing-positive armor deliberately removed) — the plan's own justification 'gold is near-complete within each axis' explicitly does not extend to replay. Verified plan lines 96-97 say 'original noisy labels' verbatim. Modified: the domain critic's ablation must be selected on CAL, not SEALED (sealed one-shot discipline), and the confidence filter must use pre-refit raw-score quantiles since Stage D calibration runs after Stage C; forward-correction transition matrices are dropped as excess machinery.

[ADOPT-MODIFIED] (major) [literature] In-sample plateau scoring loses recall to memorization; EMA/snapshot ensembling is near-free (lit-2)
    Refutation attempt: the plan already states the in-sample caveat and carries three mitigations. But isotonic calibration is per-tag monotone and cannot undo per-example memorization, and the failure mode (recall loss) is invisible to precision-only gates — the critic is right that this is undetectable as designed. Modified: maintaining an EMA copy in Stage A is adopted (cheap, irreversible-if-skipped), but 'mean of 3 checkpoints for candidate generation' would triple the full-corpus bill — restrict extra scorers to candidate-bearing images per the ops finding, and decide EMA-vs-plateau primacy on CAL. Downgraded to major: the failure is missed cleaning, not bad edits.

[ADOPT-MODIFIED] (major) [literature] Saerens prior-shift offset invalid under boundary oversampling and circular against sidecar priors (lit-3)
    Refutation attempt: I checked the failure direction — an understated corpus prior makes the offset too negative, which suppresses adds and swap firing, i.e. mostly CONSERVATIVE w.r.t. bad edits, costing recall. So this is not a bad-edit risk, but the ρ-correction P_sidecar/(1−ρ̂) is trivially cheap (ρ is measured in §0 anyway) and the conditional-shift point is correct (gold is selected on x within class). Modified: the random real slice for threshold validation merges with domain-4's real-only calibration into one edit rather than a separate mechanism.

[ADOPT] (major) [literature] FixRes re-adaptation covers only ~100 head rows; other 19.2K tags score at 448 through a 320 head (lit-4)
    Refutation attempt: could per-tag isotonic absorb it? No — the FixRes effect is image-dependent (apparent scale), so within-tag ranking across images changes, which no monotone map fixes; the plan's line 61 claim of 'no resolution mismatch anywhere in the decision path' is false for non-group tags. The fix is the exact published recipe (refit all rows on original labels + original ASL loss at 448 features — same data regime as training, no new noise exposure) and costs a few-hour partial feature pass plus a linear fit.

[ADOPT-MODIFIED] (major) [literature] Online AUM is O(1)/pair, not a training-loop burden; two-checkpoint diff is the documented-unreliable version (lit-5)
    Refutation attempt: is this relitigating delta 4's explicit decision? No — delta 4's own escape clause ('revisit if §5 precision comes up short') is unexecutable, because trajectories cannot be retrofitted after the run; the decision as written forecloses its own revisit. Cost math checks out (~2GB memmap, margins from logits already in the forward pass), and it is a per-image extension of the sibling-gap telemetry asl_telemetry.py already computes. Modified: guard the hook fail-open so a telemetry bug cannot compromise the run's V2-rehearsal value.

[ADOPT] (minor) [literature] SEALED underpowered for per-edge gates (lit-6)
    Correct but subsumed: merged into the gate-statistics cluster (stat-1/stat-2). The project's own golden_set_plan §3 sets ~40-50 events per must-resolve cell; the janitor plan's §3.2 sizes SEALED top-down at ~2K with no events-per-gated-rule check. The bottom-up sizing rule and pre-registered undecidable-vs-failed distinction are folded into top_changes #3.

[ADOPT] (critical) [statistics] Gates carry no binomial sampling-error term (stat-1)
    Refutation attempt: none survives — §3.1/§6 literally never reference n, and at SEALED-realistic n the sampling band (~0.12 at n=30) dwarfs ε_gold (~0.02-0.03). Verified the certification arithmetic (0.05^(1/29)=0.902; 0.05^(1/59)=0.9505). A gate without a confidence bound at these n's is decorative; this is a straight omission, not a design choice.

[ADOPT-MODIFIED] (critical) [statistics] Per-edge swap gates impossible: 548 edges vs ~1.9K images, and SEALED has no wrong labels to fire on (stat-2)
    Refutation attempt made it WORSE: on truth-labeled SEALED, any swap-rule firing is by construction a wrong proposal (GT sibling i is true), so organic SEALED swap precision measures ≈0 — the gate isn't just underpowered, it is structurally meaningless. Synthetic corruption (flip gold labels per the corpus confusion prior, score proposals against gold truth) is the standard fix and manufactures per-edge n. Modified: dropped the hierarchical Beta-Binomial shrinkage as reporting-only complexity, and added the caveat that random flips are easier than real correlated errors, so synthetic precision is optimistic — the real pre-writeback audit remains the certifying gate.

[ADOPT] (major) [statistics] ±ε_gold symmetric band is the wrong error propagation (stat-3)
    The Rogan-Gladen algebra is correct, α and β are separately measurable in the already-planned spot audits at zero extra cost, and the correction is usually LESS pessimistic than blanket subtraction — this buys back auto-apply volume while being more correct. No refutation available; folded into the gate-redefinition edit.

[ADOPT] (major) [statistics] Per-tag isotonic at 10-80 CAL positives will overfit (stat-4)
    Arithmetic verified from the plan's own split table (CAL ≈ 20% of 9,465 across 4-6 axes → 10-65 positives/tag); isotonic's flat-extrapolation tails sit exactly in the extreme-score region where τ_add lives. The fix REDUCES complexity (per-group parametric + per-tag intercept merges naturally with the prior-shift offset into one additive term), so there is no complexity-cost ground for rejection.

[ADOPT-MODIFIED] (major) [statistics] No multiplicity control; audit lacks power against moderate failures; false-rollback risk (stat-5)
    The arithmetic is right (K=30 gates at 95% → 1.5 expected false passes; n=200 audit has ~0.34 power against a 0.95→0.93 drop). Volume-weighted α targets expected bad-edit VOLUME — exactly the user's loss function — without Bonferroni-starving small rules. Modified only in that the audit redesign merges with domain-3's pre-writeback audit (one audit, before writes, exact-binomial pre-registered count) rather than remaining a post-gate backstop.

[ADOPT] (major) [statistics] Winner's curse in τ fitting on CAL (stat-6)
    Standard selection-on-noise argument, correct at the plan's n_fired ≈ 30-80: min-τ-meeting-bar conditions on positive noise, so genuinely-good rules fail the one-shot SEALED gate at roughly coin-flip rates and are wastefully demoted. Requiring the CAL lower bound (not point estimate) to clear the bar is definitional, costs only recall, and the recall cost is honestly reportable.

[ADOPT] (minor) [codebase] Pos-embed citation wrong; runtime interpolation makes the explicit switch step near-moot (code-1)
    Verified directly: training_utils.py:1787-1841 is the vocab-SHA guard; the interpolation is at 1868-1925, and model_architecture.py:456-481 interpolates at runtime. Adopt the citation fix and pin option (a) (construct the 448-config model, let checkpoint load interpolate once) for determinism and per-batch cost.

[ADOPT] (major) [codebase] Registry covers 4 of 6 named groups; penis axis cannot be a swap group (code-2)
    Verified: confusable_groups.json has exactly hair_color/eye_color/hair_length/breast_size, and its header scopes it to sibling-gap telemetry semantics. golden_set_plan §4.3 establishes the penis axis is PU (92.2% unlabeled-normal, small_penis n≈8) — swap semantics are structurally inapplicable. §8 item 2 as written ('confirm coverage') would fail; the task is authorship, not confirmation. Merges with domain-6 into a rule-typed cleaning registry.

[ADOPT] (critical) [codebase + ops] 'Existing writeback path' has no provenance or reversibility — §6's safety claim is new code (code-3 + ops-4, merged)
    Verified the docstring: 'no backups, no DB writes.' Both critics converge; the ops critic's APPLIED-delta journal spec wins the merge because an intent ledger cannot invert edits (rolling back a no-op add would delete a pre-existing tag) — the journal must record actual mutations plus the pre-edit tag list. Upgraded to critical: reversibility is the single insurance on the plan's stated irreversible failure mode, and it currently does not exist.

[ADOPT] (minor) [codebase] Loss-state persistence already implemented in the working tree (code-4)
    Verified in asl_telemetry.py (checkpoint-wins restore semantics, state shared by reference into TrainingState.loss_state) and git status (uncommitted). Good news, not a defect: §8 item 1 changes from 'build' to 'commit + kill/resume test asserting γ_neg restores from checkpoint, not YAML.'

[ADOPT] (major) [codebase] Inference_Engine force-overrides image_size back to 320; the 448 scorer is a real build (code-5)
    Verified Inference_Engine.py:596-604: config.image_size is silently overwritten with the checkpoint's trained value, which would violate the plan's own never-mix-resolutions rule on the very first scoring batch. The emit stage (parquet with group columns + GT columns) also doesn't exist. The scorer must load via CheckpointManager into a 448-config model directly and is a days-scale task, not an adaptation.

[ADOPT] (minor) [codebase] forward() returns logits only; feature cache needs a hook; refit-bias/prior-offset double-count trap (code-6)
    Verified model_architecture.py:720-723 (dict with logits only) and the single-Linear tag_head (so a forward hook on tag_head captures the post-norm CLS feature with zero model changes). The double-count warning (refit rows get new biases; the prior-shift intercept must apply once, after refits) is a real subtle trap worth one sentence.

[ADOPT] (minor) [codebase] logits[:, 2:] slicing is the wrong idiom; mask by resolved index (code-7)
    Verified: no existing consumer slices; all skip PAD/UNK by index. Positional slicing would shift every stored parquet column by 2 relative to the vocab and the group index tensors asl_telemetry builds — a silent off-by-two across every candidate list. One-sentence fix, adopt.

[ADOPT] (major) [codebase + ops] Early-checkpoint cross-check: scope to candidate images and gather dense candidate cells (code-8 + ops-3, merged)
    The ops framing is the decisive one: it is not just a 2× cost issue — an identical top-256 schema from the early model would be missing the plateau model's candidates precisely on the suppressed tags the signal was retained to rescue, so the boost silently degrades to 'absent = no boost.' Candidates-first ordering with a dense gather on candidate images fixes correctness and cost simultaneously.

[ADOPT] (critical) [ops] save_total_limit=3 deletes both checkpoints the plan depends on (ops-1)
    Verified configs/unified_config.yaml:276. The plan says to run Stage A 'exactly' per standing config, mandates keeping the epoch-8-10 checkpoint, and never touches retention — the early checkpoint is pruned ~30 epochs before the branch and 'select among plateau-region checkpoints' has only the last ~3 to select from. Unexecutable as written; irreversible once the run passes epoch ~13; must be a Stage-A delta.

[ADOPT] (critical) [ops] Top-256 parquet cut on raw rank starves M1 — the plan's strongest mode (ops-2)
    Refutation attempt: are per-tag transforms really rank-breaking across tags? Yes — isotonic and the prior offset are per-tag and heterogeneous (boundary-oversampled gold makes offsets large), so cross-tag rank on raw sigmoids ≠ rank on decision scores; a rare tag raw-ranked 300th can be the top calibrated candidate and never reach the artifact. Stage D precedes Stage E in the plan's own lifecycle, so in-line calibrated persistence is free; the τ−0.1 slack additionally preserves post-SEALED re-thresholding without a 6.3M re-score.

[ADOPT] (major) [ops] Uniform audit blind in tail deciles; no gate between discharge and next-run launch (ops-5)
    Power math checks (~10-20 of 200 uniform samples land in the bottom decile — blind exactly where §0-ρ says noise concentrates), and the catastrophic path (bad decile discovered weeks into the next from-scratch P1) currently has neither detection power nor a recovery procedure. Decile-quota stratification, a third user decision point, and journal retention until the next run's early canary/EPR evals look sane are all cheap procedure.

[ADOPT] (minor) [ops] Scoring bill is 10-18h/pass, not weeks; TTA and early-pass multipliers must be priced (ops-6)
    The FLOP arithmetic and 5090 throughput are rough but order-of-magnitude sound (consistent with the ~7s/img generation numbers elsewhere implying ample inference headroom). Replacing 'budget accordingly' with numbers and giving TTA a decision procedure (CAL A/B, minutes) instead of 'decide once' is strictly better.

[ADOPT] (major) [ops] §1 lifecycle contradicts §8 on split-freeze timing; canary + checkpoint selection contaminate SEALED (ops-7)
    Verified both sides: §1 places Stage B after the branch while §8 orders splits before the Stage A run, and ASL_plan §5 mandates the canary on the full 7,805-image set per eval — which overlaps the pool SEALED is later drawn from, so the checkpoint that writes 6.3M edits is partly selected on its own certification set. The fix costs nothing (freeze splits pre-Stage-A; canary keeps ~80% of the pool; selection metrics on TUNE+CAL).

[ADOPT] (critical) [domain-shift] Off-axis Anima images leak structural wrong negatives into refit and calibration (domain-2)
    Verified at the source: collection-targets line 158 and manifest.jsonl confirm sidecars carry ONLY the controlled attribute + ai-generated, while other attributes are rendered but unrecorded — a breast-bucket image enters a pooled hair-color refit as a verified negative for its actual hair color. The plan asserts within-axis completeness but has no axis-scoping mechanism (§3.2 stratifies by axis but never restricts consumption). This poisons the refit, CAL, and SEALED simultaneously; the mask-off-axis rule is one sentence.

[ADOPT-MODIFIED] (critical) [domain-shift] Auto-apply certified on ~90%-Anima SEALED; M1 adds off-axis are unscoreable on Anima (domain-3)
    Refutation attempt: the §7.2 divergence fence exists — but it can only demote on Anima-vs-real disagreement, never certify real precision, and the real SEALED slice is too small to clear any bar alone. Modified rather than wholesale: SEALED is retained as the QUALIFYING gate (eligibility, thresholds, within-group structure — preserving the sealed one-shot design), while a pre-writeback stratified human audit of each rule's actual corpus output becomes the CERTIFYING gate — repurposing and resizing §6's existing 200-edit audit rather than adding a new mechanism.

[ADOPT-MODIFIED] (major) [domain-shift] Isotonic fit on Anima-dominated CAL won't transfer; fence is detect-and-discard (domain-4)
    The covariate-shift point stands (locked generator config, single resolution, style scaffold; Ovadia 2019 on post-hoc calibration under shift), and splitting calibration by purpose (real data for absolute thresholds, Anima for within-group relative quantities) is the right resolution. Modified: the fence upgrade is capped at ONE recalibration on accumulated real audit labels then demote — an open refit-and-recheck loop risks becoming iterative peeking, which the plan's one-pass discipline correctly forbids.

[ADOPT-MODIFIED] (major) [domain-shift] Anima-style shortcut can inflate canary-based checkpoint selection (domain-5)
    Partially refuted: 2 of the 3 §2.2 selection criteria (per-decile EPR, sibling-gap) already run on real validation data, so 'every measurement shares the distribution' overstates it. But the shortcut mechanism is plausible (2,400 same-generator images in the P1 pool, ai-generated tag in sidecars) and the fix is nearly free: compute selection on TUNE+CAL real images too and report Anima-vs-real rank agreement across plateau checkpoints, with real ranking winning disagreements. Adopted in that reduced form, folded into the ops-7 edit.

[ADOPT] (major) [domain-shift] Ordinal groups in swap auto-apply contradict the M2 scope fence (domain-6)
    Refutation attempt: are adjacent ordinal swaps really M2? Yes — medium→long adjudicated by a booru-convention backbone against convention-shared Anima ordinal gold (collection-targets §8 caveat verbatim) is exactly the diagonal blind spot §7.1 declares out of reach; §5's rule keyed on 'a registered group' silently includes hair_length and breast_size. Genuine internal contradiction. Far (non-adjacent) ordinal disagreements may still route to human queue — those are gross mislabels, not convention calls.

=== TOP CHANGES (11) ===
--- #1: Replace the fictional 'existing reversible writeback path' with an applied-delta journal, and gate the next run's launch on it (section §6 (auto-apply row) + §8 item 4 + user decision points)
WHY: This is the direct insurance on the stated irreversible failure mode. The cited applier (L:/Dab/DataCleaning Project/apply_corrections_fast.py) is verbatim 'no backups, no DB writes'; §6's reversibility claim is currently new, unbudgeted code, and an intent log cannot invert edits (rolling back a no-op add deletes a pre-existing tag).
EDIT: Rewrite the §6 auto-apply disposition: writes go through a NEW tool (port apply_corrections_fast.py's format-preserving sidecar editor into OppaiOracle/tools/janitor_apply.py). The applier writes an append-only APPLIED-delta journal (NDJSON): one record per ACTUAL mutation — {image_id, tag, op(add/swap_from/swap_to), rule_id, calibrated score, model='janitor-v1', registry+calibration version, batch_id, full pre-edit tag list, timestamp} — with no-ops logged as no-ops. Application is idempotent and resumable (6.3M small NTFS writes take hours and can be interrupted mid-flight). Rollback = inverse replay of the journal filtered by rule_id/batch_id using the recorded pre-edit tag lists; write the recovery runbook (rollback → cache rebuild → restart) into §6. Retention: journal + pre-clean state retained until the NEXT production run's first Anima-canary and per-decile-EPR evals look sane. Add a THIRD user decision point: next-run launch requires the discharge report reviewed. §8 item 4 changes 'writeback provenance fields' to this applier+journal as a real, days-scale build task.

--- #2: Make a pre-writeback, decile-stratified human audit of each rule's real corpus output the certifying gate; SEALED becomes the qualifying gate (section §6 (gates + audit sample))
WHY: SEALED is ~90% Anima and cannot certify real-domain precision; M1 adds outside a bucket's own axis have no Anima ground truth at all; and the current post-gate 200-uniform audit is blind in the tail deciles where §0-ρ says noise concentrates. Moving the human look BEFORE any sidecar write is the strongest cheap protection against bad edits.
EDIT: Restructure §6 as a two-stage gate. SEALED (with the Edit-3 statistics) QUALIFIES a rule: eligibility, threshold verification, within-group structure — reported per domain (Anima vs real), never pooled. The CERTIFYING gate is a pre-writeback human audit of the rule's ACTUAL corpus candidates: stratified sample with a fixed quota per tag-frequency decile (e.g. 40 per super-decile, ≥200 total; 500 for rules proposing >100K edits), reviewed in the portal before any write. Auto-apply only if the audit's one-sided exact-binomial lower bound (95%; 99% for >100K-edit rules) clears the bar after gold/reviewer-error correction; pre-register the rejection threshold as a COUNT (e.g. n=200 vs a 0.95 bar at α=0.01 → reject at ≥19 failures), not a proportion-vs-band comparison. This replaces (not supplements) the current post-gate 200-edit audit. The audit labels double as real-domain calibration data for Edit 8.

--- #3: Rebuild the gate math: exact-binomial lower bounds, α/β gold-error correction, minimum-n eligibility, per-group pooling, damage-weighted alpha (section §3.1 + §3.2 + §6)
WHY: The gates currently carry NO sampling-error term — at n=30 the binomial band (~0.12) dwarfs ε_gold (~0.02-0.03) — and the symmetric ±ε_gold band mis-propagates gold error. A perfect rule with 20 SEALED events cannot mathematically pass a 0.90 bar; without a min-n rule, gate outcomes are sampling noise.
EDIT: Redefine every gate: one-sided 95% Clopper–Pearson (or Jeffreys) LOWER confidence bound on measured precision, then gold-error correction via p_true = (p_L − α_hi)/(1 − α_hi − β_lo) (Rogan–Gladen), where the ε_gold spot audits (§8 item 3) record gold false-positive rate α and false-negative rate β SEPARATELY per axis (same audit, two counters — usually LESS pessimistic than blanket ±ε subtraction, buying back automation). Minimum-evidence rule: a rule whose evaluable decision count cannot mathematically certify its bar (n<29 zero-error for 0.90; n<59 for 0.95) is ineligible for auto-apply and pre-registered human-queue-only — reported as 'undecidable', never 'failed'. Gates pool per-GROUP (never per-edge below the n floor). Damage-weighted multiplicity control: rules proposing >100K edits certify at the 99% lower bound (α=0.01), small rules keep 95% — this bounds expected bad-edit VOLUME without Bonferroni-starving every gate. Size SEALED bottom-up from the enumerated gate list (≥40 expected decisions per gated unit).

--- #4: Restrict swap auto-apply to nominal color groups, evaluate swaps by synthetic corruption, and author a rule-typed cleaning registry (section §5 (swap rule) + §6 (per-edge gate) + §8 item 2)
WHY: Two independent fatal problems converge here. (1) The swap rule fires on wrong stored labels, but SEALED sidecars are truth — any organic SEALED firing is a wrong proposal by construction, so measured swap precision ≈ 0 and the per-edge gate is meaningless as designed. (2) §5's rule keyed on 'a registered group' includes the ordinal axes, contradicting the §7.1 M2 fence: adjacent ordinal swaps certified against convention-shared Anima gold is the janitor adjudicating its own convention. Also verified: confusable_groups.json has only 4 of the 6 named groups, and the penis axis is PU (no swap semantics possible).
EDIT: (a) §5 swap row: auto-apply swaps/removals are restricted to the NOMINAL groups (hair_color, eye_color); ordinal groups (hair_length, breast_size) emit calibrated columns for the DINOv2 fusion track plus at most human-queue proposals for FAR (non-adjacent) disagreements; the penis axis is detection/M1-add-only (PU per golden_set_plan §4.3 — no 'normal' tag to swap to); multi-color is a label-set track (set-overlap rule, not swap). (b) §8 item 2 changes from 'confirm coverage' to AUTHOR configs/cleaning_groups.json: keep confusable_groups.json for telemetry, and give the cleaning registry per-group rule-type metadata {nominal-swap | label-set | PU-add-only | ordinal-columns-only} plus allowed edges, so the rule runner cannot silently pick up ordinal edges. (c) §6 swap evaluation: build the swap-eval set by SYNTHETIC CORRUPTION — take gold-labeled SEALED images of true class j, flip the presented sidecar label to sibling i (sampling flips from the corpus confusion prior), and score the rule's proposals against gold truth; this manufactures arbitrary per-edge/per-group n from the ~405-image hair-color stratum. Report the caveat that random flips are easier than real correlated errors (optimistic estimate) — the Edit-2 real audit remains the certifying gate.

--- #5: Axis-scope all Anima gold: a group consumes only its own bucket axis; off-axis Anima tags are MASKED, never negative (section §3.2 (splits) + §3.3 (refit) + §4 (CAL) + §3.1 (ε_gold audit))
WHY: Verified at the source (collection-targets line 158 + manifest.jsonl): each Anima sidecar carries ONLY the controlled attribute + ai-generated, while other attributes are rendered but unrecorded. A breast-bucket image with rendered aqua hair enters a pooled hair-color refit/CAL/SEALED as a verified negative for aqua_hair — structural wrong negatives inside the exact groups being fixed. The plan asserts within-axis completeness but has no enforcing mechanism.
EDIT: Add an explicit rule to §3.2/§3.3: group G's refit, calibration, and SEALED evaluation use only Anima images from G's own bucket-axis folders (folder = complete-for-that-axis label); for every OTHER Anima image, G's tags are treated as MISSING (masked in the refit loss and excluded from CAL/SEALED denominators), never as negatives. Optional recovery: parse .research/golden_gen/manifest.jsonl prompts to harvest prompted secondary attributes as low-trust positives only — never as verified negatives (uncontrolled attributes were never screened). Extend the §3.1 ε_gold spot audit with a cross-axis check (e.g. does a prompted-hair-color image contain a second hair color) so within-axis completeness is measured, not assumed. Note the consequence for gates: an M1 add for tag t is verifiable on Anima only within t's own axis stratum, reinforcing per-group pooling (Edit 3) and real-audit certification (Edit 2).

--- #6: Stage-A irreversibles package: archival checkpoint retention, EMA copy, online AUM, and splits frozen (SEALED-excluded canary) before launch (section §1 (lifecycle order) + §2 deltas 2-4 (new deltas 5-6) + §3.2 + §8 items 3/5)
WHY: Everything in this bundle is impossible to retrofit after Stage A. Verified: save_total_limit=3 (unified_config.yaml:276) prunes the epoch-8-10 checkpoint ~30 epochs before the branch and leaves only ~3 checkpoints to 'select among'; the ASL canary runs on the full 7,805-image set every eval, so the branch checkpoint would be partly selected on images that later certify it; delta 4's 'revisit if precision comes up short' clause is unexecutable because trajectories cannot be recomputed post-run.
EDIT: (a) New §2 delta 5 — retention: pin the epoch-8-10 checkpoint by copying it OUT of CheckpointManager's managed directory, and archive model-only (optimizer-stripped, ~770MB) copies every epoch from plateau onset, noting this as a benign divergence from the V2 rehearsal. (b) New §2 delta 6 — maintain an EMA weight copy through the plateau region (one weight copy, ~free at train time); the plateau checkpoint is selected at maximum memorization (Kim 2022) and snapshot/EMA self-ensembles beat single converged models at label-error detection (SELF, ICLR 2020); decide on CAL whether EMA replaces or averages with the plateau scorer for candidate GENERATION, keeping the gold-refit head for gated decisions. (c) Rewrite delta 4: log online AUM — running mean margin per (image, GT-positive tag) and per registered group column, computed from logits already in the training forward pass (~2GB memmap, zero extra passes; a per-image extension of the existing sibling-gap telemetry in asl_telemetry.py); primary suppression-resistance feature for M1 and primary ranker for wrong-positive candidates; hook guarded fail-open so a telemetry bug cannot kill the rehearsal run. (d) Fix §1 to match §8: TUNE/CAL/SEALED are frozen BEFORE Stage A launches; exclude SEALED images from the training-time Anima canary (canary keeps ~80% of the pool, trend role unaffected); compute §2.2 checkpoint selection on TUNE+CAL only, add one real-data criterion (per-group sibling-gap/off-diagonal on TUNE+CAL portal reals), and report Anima-vs-real rank agreement across plateau checkpoints — real ranking wins disagreements (guards the 2,400-Anima-in-training style shortcut).

--- #7: Redesign the replay mix: down-weight, filter or distill the contested group columns — never raw noisy labels at 50% under plain BCE (section §3.3 (refit mechanics))
WHY: As written, half of each refit batch trains the ONLY trainable rows on the exact M1/M3 noise being cleaned, with ASL's missing-positive armor deliberately removed — a DFR-assumption violation (Kirichenko requires a clean reweighting set), and the plan's 'gold is near-complete within each axis' justification does not extend to replay. The refit converges to a gold/convention compromise, shrinking the boundary shift that is the entire point.
EDIT: Rewrite the replay bullet: replay images remain (real-domain anchoring), raw noisy supervision on contested columns does not. Mechanism: (i) down-weight the replay loss term to λ≈0.25 (~4:1 gold:replay effective weight); (ii) for registered-group columns, keep a replay supervision pair only when the pre-refit model already agrees with its label (labeled sibling in the top score quantile ≈ p>0.9-equivalent, unlabeled siblings low — small-loss selection per Co-teaching; use raw-score quantiles since Stage-D calibration runs after Stage C), OR replace group-column replay targets with pre-refit soft outputs (self-distillation) — both pin the real-domain operating point without pulling the contested boundary; (iii) because refits run on cached features, ablate {raw, filtered+down-weighted, distilled} and select on CAL (never SEALED) by off-diagonal metric + Anima-vs-real precision gap. Non-group columns of replay images may keep original labels (they are not being refit).

--- #8: Replace per-tag isotonic with per-group parametric calibration + one per-tag intercept; ρ-correct the corpus prior; fit thresholds with a winner's-curse margin on real data (section §4 (all three items))
WHY: CAL supplies ~10-65 positives per tag — isotonic is unreliable below high hundreds and its flat tails sit exactly where τ_add lives, so the 'Platt fallback for positive-starved tags' inverts reality. The Saerens offset's target prior is understated by exactly the missing positives being cleaned (circular), boundary oversampling violates its label-shift assumption, and min-τ-meeting-bar threshold fitting selects on upward noise so good rules fail SEALED at coin-flip rates.
EDIT: Rewrite §4: (1) Default calibration = per-GROUP temperature/vector scaling (beta calibration if reliability curves are non-sigmoid) + a per-tag additive intercept, merged with the prior-shift offset into ONE per-tag logit shift; per-tag isotonic permitted only at n_pos≥200 on CAL (state explicitly: approximately nowhere). (2) Correct the corpus prior before use: P̂_true(y) = P_sidecar(y)/(1−ρ̂_decile) from the §0 ρ measurement — the raw sidecar prior makes the offset too negative exactly where ρ is highest, suppressing adds and under-firing swaps. Treat the offset as initialization/diagnostic only, since boundary-oversampled gold is a conditional shift outside the Saerens assumption. (3) Absolute decision thresholds (τ_add/τ_swap) are validated on REAL data: CAL-real portal images plus a small prevalence-representative random real slice (~300-500 portal-reviewed, NOT boundary-oversampled) plus the Edit-2 audit labels as they accumulate; CAL-Anima is used for within-group relative quantities (swap margin δ, sibling ranking), which survive monotone domain shift. (4) Winner's-curse guard: choose τ so the CAL Jeffreys/CP LOWER bound clears the bar — i.e. target point precision ≈ bar + 1.65·√(bar(1−bar)/n_fired) (≈0.98 at n=50 for a 0.95 bar); accept and report the recall cost. (5) §7.2 fence upgrade, capped: on SEALED-real divergence, ONE recalibration on accumulated real audit labels is permitted, then demote — no iterative refit loops.

--- #9: Fix the Stage-E artifact and schedule: persist threshold-relative CALIBRATED scores (not raw top-256), candidates-first early-checkpoint pass, scorer built outside ModelWrapper, index-masking hygiene, priced defaults (section §5 (scoring run + storage + cost note) + §8 item 4)
WHY: Three verified breaks: the top-256 cut on raw scores silently starves M1 (per-tag isotonic + heterogeneous prior offsets reorder tags across images, so the plan's strongest mode loses exactly its rare-tag candidates); an identical-schema early-checkpoint parquet won't contain the plateau model's candidate pairs (the boost degrades to 'absent' on the suppressed tags it exists to rescue); and Inference_Engine.py:596-604 force-overrides image_size back to the checkpoint's 320, violating never-mix-resolutions on the first batch.
EDIT: (a) Storage: the scoring harness applies calibration + prior offsets IN-LINE (Stage D precedes E) and persists every (tag, p_cal) with p_cal > τ_add(tag) − 0.1 slack — so thresholds can be revised post-SEALED without a 6.3M re-score — plus all registered group columns and all GT=1 columns; calibrated top-K kept only as a diagnostic. (b) Schedule: plateau/EMA scorer emits M1 candidates first; the early checkpoint then scores ONLY candidate-bearing images and gathers exactly the candidate (image,tag) cells dense from its full logits; promote the early-checkpoint cross-check from 'optional signal' to a required input of the M1 rule. (c) Build: the scorer loads via CheckpointManager into a 448-config SimplifiedTagger directly (bypassing ModelWrapper's preprocessing override); reuse InferenceDataset/preprocess_batch; new parquet emit stage; budget as days in §8 item 4. Capture head-input features via a forward hook on model.tag_head where needed (forward() returns logits only). (d) Hygiene: mask special indices {PAD_idx, UNK_idx} resolved from the vocab, never positional logits[:,2:] slicing; all parquet columns store ORIGINAL vocab indices. (e) Cost note: ~315 GFLOP/image at 448 → roughly 100-200 img/s on the RTX 5090 → ~10-18h per full 6.3M pass (bf16, no grads, batch ≥192); flip-TTA default OFF, decided by an A/B of gate precision on CAL at 448 (a few-thousand-image job, minutes).

--- #10: Add a whole-head FixRes re-adaptation pass so all 19.3K tags — not just the ~100 refit rows — are resolution-adapted at 448 (section §2 delta 1 + §3.3 + §4.1)
WHY: Touvron's fix is per-classifier, not per-subset-of-classes: the M1 add rule thresholds 448 scores for ALL tags, but only the group rows get re-adapted; monotone per-tag calibration cannot repair FixRes's image-dependent within-tag ranking distortion, so non-group M1 precision/recall silently pays — and the plan's line-61 'no resolution mismatch anywhere in the decision path' claim is currently false for 99.5% of tags.
EDIT: Insert between the branch and the §3.3 gold refit: sample ~200-500K corpus images, run the frozen backbone once at 448, cache pooled CLS features (~0.9GB fp16 via the tag_head forward hook), and briefly refit ALL head rows on those features with the ORIGINAL labels and the ORIGINAL ASL loss — pure resolution re-adaptation in the same data regime as training, so no new label-noise exposure (the exact Touvron recipe: adapt only the classifier at target resolution). The §3.3 gold group refit then applies ON TOP of the resolution-adapted head. Cost: a few-hour partial feature pass + a linear fit over cached features — negligible next to the 6.3M scoring bill. Add the ordering note: the whole-head refit and the group refit both re-derive biases; the §4.2 prior-shift intercept is applied once, after all refits, never double-counted.

--- #11: Small verified corrections: pos-embed citation, loss-state status, resume test (section §2 delta 1 + §8 item 1)
WHY: Keeps the doc honest against the verified codebase and converts one §0 blocker from 'build' to 'commit + test' — the cheap-failure rehearsal value the plan claims depends on this code being exercised before Stage A.
EDIT: (a) §2 delta 1: cite training_utils.py:1868-1925 for the checkpoint-load interpolation (1787-1841 is the vocab-SHA resume guard) and note model_architecture.py:456-481 provides runtime interpolation; for the scoring model, construct with image_size=448 and let checkpoint load interpolate once (deterministic, no per-batch cost). (b) §8 item 1: loss-state checkpoint persistence is ALREADY IMPLEMENTED in the uncommitted working tree (asl_telemetry.py restores γ_neg from checkpoint with checkpoint-wins-over-YAML semantics; TrainingState.loss_state shared by reference) — reword the task to: commit the working-tree implementation + add a resume test that kills a run mid-descent (γ=6), resumes, and asserts the criterion's γ_neg equals the checkpoint value, not YAML.
