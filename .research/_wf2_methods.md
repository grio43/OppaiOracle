### Geometric measurement of anime hair length: a 3-stage CV pipeline — (1) anime/illustration 2D keypoint pose estimation (Chen & Zwicker WACV 2022 "bizarre-pose-e
VERDICT: A genuine but NARROW bias-break: where it fires its error is truly orthogonal to the Danbooru convention (geometric jitter, not annotator default), making it one of the few non-circular, non-VLM anchors available — and on the clean full-body/hair-down slice, when it AGREES with the opposite-biased VLM it is strong cross-family evidence to auto-upgrade long->very_long. But this corpus's own framing tags show it can cleanly measure only ~9% of the M4 upgrade pool; ~80% abstains because the hip landmark is off-frame or the hair is tied up, and that abstention is anti-correlated with the very_long cases that matter most. So it is NOT the primary bias-break for the majority; deploy it as a high-precision SPECIALIST CONFIRMER — a third independent annotator in the IBCC/DBCC fusion, scoped to the measurable slice with aggressive abstention and gold-calibrated per-direction confusion — and build it only after the gold set + rubric exist, since both its R-cutoffs and its abstention gate depend on them.
ORTHOGONAL: PARTIALLY orthogonal — strong on direction, weak on coverage, with one residual correlation that must be acknowledged. PRO: the convention bias is annotator-cognitive ("long_hair is the reflexive default; medium skipped; refinements omitted") and identical across all Danbooru-derived models. A geometric pipeline's error sources are different in KIND: keypoint localization error (PCKh@50 ~78.7% on illustrations -> ~21% of joints outside tolerance, but error is ~zero-mean jitter around the true joint, not a directional push toward "long"), segmentation boundary error, and landmark-scale noise. N
VIABILITY: Honest expectation: viable as a HIGH-PRECISION, LOW-COVERAGE specialist on the standing/hair-down minority; NOT viable as the primary bias-break for the M4 majority. Measured on THIS corpus (70K-sidecar scan, L:/Dab/OppaiOracle/.research/ma_framing_scan.py), among the M4 upgrade subject pool (solo + any longish-hair tag, n=28,005): only 9.44% are "cleanly measurable" (full_body OR cowboy_shot AND not tied/back/seated/chibi); 35.7% have tied-up hair (ponytail/twintails/bun/braid -> can't measure let-down length); 68% carry NO framing tag at all (Danbooru default crops hips off-frame); only ~10.
SCALE/COST: Feasible compute, but you would NEVER run it on 6M — and shouldn't. Two-model inference (ISNet seg ~3 ms/img + pose estimator + a hair parser) is realistically ~50-150 ms/img on one modern GPU; at the high end ~6M x 0.15 s ~= 10 GPU-days, low end ~2-3 GPU-days — cheap and one-time. BUT the corpus measurement kills the case for running it broadly: ~80% of the M4 pool abstains for missing landmarks/

### ENTITY/CHARACTER-CANONICAL-ATTRIBUTE denoising: resolve each named character's canonical hair length ONCE (via a directional-UPPER consensus over all their inst
VERDICT: A strong, cheap, genuinely complementary bias-break for the long->very_long M4 upgrade — but only in its directional-upper (MIL noisy-OR / at-least-one) form; the naive entity-MODE form inherits the convention bias and must be rejected (diversity-prediction theorem). It is partially orthogonal: the existence of very_long on a minority of a character's images is a signal the unidirectional-down bias structurally cannot fabricate, and a once-per-character wiki/community anchor adds a second cross-process signal. Its role: a precise, near-zero-per-image FIRST PASS over the character-tagged majority (~62%+ of the M4 universe, concentrated in a few thousand head characters), feeding the gold-calibrated fusion model as one more annotator (a high-trust one in the upgrade direction) — NOT a standalone truth oracle and NOT a fix for OCs, variable-hair characters, multi-character images, or the medium collapse.
ORTHOGONAL: PARTIALLY orthogonal, and ONLY if you use the directional-upper aggregator — the naive version INHERITS the bias and must be rejected. Proof both ways. (A) Why the MODE inherits the bias: the diversity-prediction / wisdom-of-crowds theorem states aggregation removes the VARIANCE (random, per-image) component of error but CANNOT remove a SYSTEMATIC component shared across the instances ("averaging eliminates random errors that affect each answer differently, but not systematic errors that affect the whole crowd the same way"; Hovy/Davani-style annotator-GROUP-bias work says the same for correla
VIABILITY: Strong on the anime domain BECAUSE it never looks at a pixel — it operates entirely on the GT tag co-occurrence structure, so it is immune to the VLM failure modes (anime-OOD, relative-spatial measurement, granularity collapse) that killed the per-image VLM oracle. Accuracy expectations are honest and asymmetric: (1) For HEAD characters with a stable canonical long/very_long design and a large bag (>=30 imgs), the very_long-rate signal is clean and propagation precision should be high (the 71-86% intra-bag rates for formidable/daiwa/raikou are essentially the character's design asserting itsel
SCALE/COST: Highly feasible and the cheapest of the candidate methods at 6M scale. COVERAGE (measured on a 200-300K corpus sample): ~62% of solo-with-length images carry a disambiguated character tag (broad lower bound; misses bare-name characters, so true named-character coverage is higher, with ~13% explicitly 'original' OCs that are structurally non-propagable). The within-character image distribution is a

### Noise-robust training as an alternative to cleaning 6M labels: a SORD ordinal-soft-label head for hair-length + ASL gamma-asymmetry inversion, with a gold-ancho
VERDICT: STRONG as a COMPLEMENT, WEAK as a STANDALONE bias-breaker — and that distinction is the whole answer. Noise-robust training is the correct mechanism for the user's actual symptom ('the model recommends wrong tags') because it fixes the v2's predictions corpus-wide at near-zero marginal cost, covering the silent majority that per-image cleaning can never reach. But the transition-matrix half is NOT independent: its consistency guarantee is void under M4's directional, instance-dependent, shared-bias noise (Xia/Yao unidentifiability), so any T estimated from the corpus or the model itself LAUNDERS the bias with false confidence — T-correction is admissible ONLY with off-diagonals estimated from the external gold/VLM/community anchor (GALC-SLR-style), where it becomes a powerful amplifier of that orthogonal signal across all 6M images. The genuinely domain-robust, do-this-first pieces are the cheap loss-geometry fixes: invert the current gamma_neg=7.0 / apply SPLC on the fine-end length tags (it is actively training IN the under-tag bias today) and add a SORD ordinal soft-head so adjacent confusions are cheap and the fuzzy boundary is predicted honestly-soft instead of confidently-wrong. ROLE: run M-C as the corpus-wide v2 training fix that CONSUMES the gold set produced by the cleaning track — robust training and partial cleaning are multiplicative (cleaning supplies the mathematically-required external anchor; robust training spends it across the whole corpus), not competing alternatives.
ORTHOGONAL: PARTIALLY ORTHOGONAL, AND ONLY UNDER A HARD PRECONDITION — this is the crux and I will not overstate it.

The forward/backward correction's consistency guarantee (Patrini CVPR'17) rests on class-conditional noise: P(noisy|true,x)=P(noisy|true). M4 violates this twice — the noise is instance-dependent (the long/very_long boundary is a feature-dependent perceptual judgment) AND systematically directional. The Xia/Yao unidentifiability theorems (NeurIPS'20/'21) and the part-dependent-T result prove you CANNOT estimate T from the noisy corpus alone — anchor-point estimation (Patrini's default) and
VIABILITY: VIABLE on anime, with realistic ceilings, BECAUSE this track never asks a model to PERCEIVE hair length per-image — that is the part anime/OOD breaks (VLMs at ~50% on relative-spatial per BLINK). 

What actually runs on anime here: (a) inverted-ASL/SPLC and SORD operate purely on the LABEL geometry and the loss, fully domain-agnostic — they work identically on anime as on COCO; no perception of the image is required, so stylized art/occlusion/tied-up-hair/AUs are irrelevant to them. They will reliably stop the v2 from CONFIDENTLY recommending long-only and will make adjacent (long/very_long) e
SCALE/COST: FEASIBLE and the CHEAPEST of all M4 tracks at 6M scale, because it adds essentially ZERO per-image cost — it is a one-time v2 retrain you are already planning, with modified loss/targets.

- Inverted-ASL + SPLC (track 2): ~6-index config change + ~30 lines of per-tag loss masking. Marginal cost ~0; folds into the existing v2 run. Highest ROI.
- SORD sub-head (track 3): one small softmax head + sof

### SPECIALIST SMALL ORDINAL MODEL = a frozen non-Danbooru self-supervised backbone (DINOv2 ViT, Oquab et al. 2024) + a lightweight rank-consistent ordinal head (CO
VERDICT: Strong bias-break - the best non-VLM, non-circular candidate of the five, precisely because its only correlated-error channel (the gold set) is the one thing you fully control, unlike the architecturally-biased Danbooru taggers and the anime-OOD/spatial-blind VLM. Frozen DINOv2 features are documented to carry part-level semantics on anime line-art (DACoN: 57.5% zero-shot, beating CLIP), and a tiny CORN/SORD ordinal head trained on a few thousand rubric-labeled gold images (grown by BADGE/core-set, reusing the gold the project already needs) should clear the failed VLMs' ~50-70% bar on the base triple while staying honestly soft at the genuinely-fuzzy long/very_long boundary. Its role: a high-quality, gold-calibrated, bias-decorrelated ANNOTATOR feeding the IBCC/DBCC fusion and the human-review queue - and the seed teacher for optional FixMatch/Noisy-Student scaling to the 6M - NOT an auto-writer and NOT a 6-level oracle. It does not replace the gold set; it is the most efficient way to turn that gold set into corpus-scale leverage.
ORTHOGONAL: Genuinely orthogonal on the dominant channel, with two residual leak channels that must be controlled - so 'orthogonal' is PROVEN for the backbone and CONDITIONAL on gold hygiene.

PROVED orthogonal (the main argument): The Danbooru convention bias lives in the LABELS (annotators reflexively tag long_hair, skip medium, omit very_long). The three external Danbooru taggers and v1 all learned a mapping image->Danbooru-tag-distribution, so their errors are correlated with that label convention by construction (Xia NeurIPS'20 / Yao NeurIPS'21 unidentifiability: any model fit to the convention decon
VIABILITY: Viable for the base triple {short, medium, long} with high confidence; fuzzy exactly where the truth itself is fuzzy (long/very_long boundary); honest about occlusion/AU failure modes.

Hard evidence DINOv2 features transfer to anime line-art at the PART level (the thing hair-length needs): DACoN (2025, arXiv:2509.14685) reports frozen DINOv2 ZERO-SHOT achieves 57.5% on anime keyframe colorization part-matching, beating CLIP 36.7%, Stable Diffusion 49.7%, SAM2 34.5% - and states explicitly 'DINOv2 captures part-level semantic information not only for natural images but also for line drawings.'
SCALE/COST: Feasible and cheap because the backbone is FROZEN - the expensive part (DINOv2 forward pass) is a one-time, parallelizable, embarrassingly-batched inference, and the trainable head is tiny.

GOLD NEEDED: realistically low-thousands of rubric-labeled images, oversampled at the boundary and the collapsed medium, grown via BADGE/core-set. Linear-probe sample-efficiency (DINOv2) + FixMatch's 4-labels/

### Maximally-leveraged human effort via three stacked leverage layers — (1) NON-Danbooru-embedding clustering + cluster/propagation annotation (label one cluster, 
VERDICT: A strong, possibly the strongest, of the surviving levers — and uniquely so now that the VLM auto-oracle is out, because it puts the only genuinely bias-independent signal (a human against the rubric) exactly where it is decisive while using clustering + entity-consensus + the community-delta to amortize that scarce human judgement across millions of images. It should be the PRIMARY mechanism for cleaning the HEAD of the corpus (entity-canonical propagation for the character head, cluster/typical-sample propagation for the characterless residual, active-cleaning to route the tail of the head to humans), with the community re-pull/delta and Danbooru's implication/alias tables as primary provenance inputs and consistency guards. Its orthogonality to the convention bias is REAL but CONDITIONAL: it holds only if propagation runs on non-Danbooru embeddings and truth is injected from human/community-delta/minority-upgrade evidence rather than averaged from biased votes — get that wrong and it launders the bias at scale. Realistically ~150-400 human-hours cleans the head (vs >25 person-years naive), with the long tail deferred to the v2 soft-ordinal retrain.
ORTHOGONAL: Each leverage layer attacks a DIFFERENT failure-component, and the human-effort layers are designed so the human (the only truly bias-independent signal) is spent where redundancy CANNOT help.

VARIANCE vs BIAS distinction is the crux. The convention bias is a SHARED, low-variance error: every Danbooru annotator/model nudges the same direction (long-as-default), so averaging more Danbooru-derived votes reduces variance but converges TO the bias (Xia/Yao unidentifiability; Gordon disagreement-deconvolution). The leverage layers are explicitly built NOT to average Danbooru votes into the truth:

VIABILITY: HONEST viability, layer by layer on stylized art:

CLUSTERING (DINOv2/CLIP on anime): WORKS for grouping, with caveats. DINOv2 has strong shape priors and clusters anime by character/style robustly in practice; the cluster does NOT need to be pure on hair-LENGTH, only coherent enough that a human's one label is right for most members. Failure: hair length is a fine-grained, occludable attribute, so a cluster keyed on character+pose can mix lengths when hair is tied up, braided, or cropped out-of-frame. Mitigation: low-purity clusters (high length-variance among members) are auto-flagged for pe
SCALE/COST: FEASIBLE at both scales, and the whole point is that this is the CHEAP path.

Compute (one-time, automatable on the existing rig): DINOv2/CLIP embedding of ~6M solo images is the main GPU cost — order of a day or two on the project's training GPU at typical throughput; embeddings are reused for clustering, entity grouping, and the v2 features. Clustering (k-means/TypiClust over 6M vectors) and the
