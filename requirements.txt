fastapi>=0.116.0  # for API (Python 3.12 compatible)
h5py>=3.12.1
imagehash==4.2.0  # optional
matplotlib>=3.8.0
networkx>=3.1  # optional
numpy>=1.26
onnx>=1.16.0,<2
orjson>=3.9.0  # fast JSON parsing for metadata cache (5-10x faster than stdlib)
# Use the GPU build of ONNX Runtime; do not install CPU + GPU together
onnxruntime-gpu>=1.22.0
onnxsim>=0.4.36
opencv-python>=4.8
pandas>=2.1
pillow>=10.0.0
pyarrow>=14.0.0  # Arrow IPC for zero-copy metadata cache (memory-mapped, shared across workers)
prometheus-client>=0.15.0  # optional
psutil>=5.9.0
python-multipart>=0.0.9  # for API
pyyaml>=6.0.1
requests>=2.31
protobuf>=4.25.3,<6
werkzeug>=2.3,<5
scikit-learn>=1.4
scipy>=1.11
safetensors>=0.4.2
seaborn>=0.13.2
tensorboard>=2.14,<2.21
bitsandbytes==0.50.2  # CUDA 13 / Blackwell 8-bit AdamW; fp32 head state stays enabled
# Upgrade these together with requirements-training-cu130.txt (Windows CUDA wheels).
# Pin the tested compiler/optimizer stack so an install cannot mix minor versions.
torch==2.14.0
torchvision==0.29.0
tqdm>=4.66.4
uvicorn>=0.35.0  # for API
wordcloud>=1.9.3  # optional

# PyTorch 2.14 pairs with Triton 3.8. Windows post28 includes SM120 fixes.
# Kernel speedups depend on model shapes and hardware; measure with -CheckTrainingStack.
triton-windows==3.8.0.post28; sys_platform == "win32" and platform_machine == "AMD64"
# On Linux, PyTorch selects its matching upstream Triton dependency.

# New training stack dependencies
torchmetrics>=1.3.0
ruamel.yaml>=0.17.31  # for config loading/writing
